schema: http://omo.local:8080/identity/graphql
documents: ./*.graphql
generates:
  generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
    config:
      rawRequest: true